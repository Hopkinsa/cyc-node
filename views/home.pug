<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content=
            "width=device-width, initial-scale=1.0")
        title Complexity Reports
        link(rel="stylesheet", href="/static/style.css")
    body
        H1 Complexity Reports

        H2 Available Codebases

        each report, idx in reports
            p
                a(href='/reports/'+idx)=report.NAME
